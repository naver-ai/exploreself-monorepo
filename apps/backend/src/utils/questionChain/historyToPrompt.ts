import { IHistoryItem } from "../../config/schemaInterface";


const historyToNL = (history: IHistoryItem[] ) => {
  const naturalLanguage = ''

  return naturalLanguage;
}

const historyToPrompt = () => {

}

export default historyToPrompt;